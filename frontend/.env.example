# Used when the frontend and backend run as separate services/hosts.
# On Render's internal network, BACKEND_HOST/BACKEND_PORT are preferred
# (see render.yaml). Fall back to BACKEND_PUBLIC_URL or localhost otherwise.
BACKEND_HOST=
BACKEND_PORT=
BACKEND_PUBLIC_URL=http://localhost:4000
