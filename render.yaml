services:
  - type: web
    runtime: node
    name: groweasy-csv-importer
    plan: free

    buildCommand: >-
      npm install --prefix backend &&
      npm run build --prefix backend &&
      npm install --prefix frontend &&
      npm run build --prefix frontend

    startCommand: bash start.sh
    healthCheckPath: /

    envVars:
      # Render will ask you to set this value
      - key: GROQ_API_KEY
        sync: false
